// Compatibility shim for older local references.
// The active implementation lives in ./extensions/supervisor-worker.ts.
export { default } from "./extensions/supervisor-worker.ts";
export * from "./extensions/supervisor-worker.ts";
